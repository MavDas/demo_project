<%= form_for [@group,@post,@post.comments.build] do |f| %>
	<p>
		<%= f.hidden_field :name, value: "#{current_user.name}" %>
	</p>

	<p>
		<%= f.label :body %><br>
		<%= f.text_area :body %>
	</p>
	<br>
	<p>
		<%= f.submit %>
	</p>
<% end %>